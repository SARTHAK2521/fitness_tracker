<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Workout</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="form-container">
        <h2>🏋️‍♂️ Add Workout</h2>

        <!-- ✅ Error Handling (Prevent ReferenceError) -->
        <% if (typeof error !== "undefined" && error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>

        <form action="/workouts/add" method="POST">
            <input type="text" name="name" placeholder="Workout Name" required>
            <input type="text" name="duration" placeholder="Duration (minutes)" required>
            <input type="text" name="type" placeholder="Type (e.g., Cardio, Strength)" required>
            <button type="submit">Add Workout</button>
        </form>

        <a href="/workouts">Back to Workouts</a>
    </div>
</body>
</html>
